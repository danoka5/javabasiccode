package Arrays;

import java.util.Scanner;

public class countries {

	public static void main(String[] args) {
		/* 
		 *  Using Scanner create an array of countries. 
		 *  When anarray is created, retrieve all values from it and while retrieving those values print capital for each country.
		 */
Scanner scan=new Scanner(System.in);
System.out.println("Enter country names");
String country1=scan.nextLine();
String country2=scan.nextLine();
String country3=scan.nextLine();
String country4=scan.nextLine();

String [] country = {country1, country2, country3, country4};
for (int i=0; i<country.length;i++) {
	System.out.println()
}
	}

}
